# 幂等性配置
idempotent:
  # Token 过期时间（秒，全局配置）
  # 优先级：注解值 > 全局配置 > 默认值（300秒，即5分钟）
  # 建议根据业务场景设置，如支付场景可设置 1800（30分钟），查询场景可设置 300（5分钟）
  expire-time: 300
  
  # 是否启用强幂等模式（全局配置）
  # true: 强幂等模式 - 重复请求返回第一次的结果
  # false: 防重模式 - 重复请求直接报错（默认）
  return-cached-result: true
