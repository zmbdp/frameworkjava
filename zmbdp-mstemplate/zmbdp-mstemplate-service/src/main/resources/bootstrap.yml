spring:
  application:
    name: zmbdp-mstemplate-service
  profiles:
    active: ${RUN_ENV}
  cloud:
    nacos:
      discovery:
        username: nacos
        password: Hf@173503494
        namespace: frameworkjava-${RUN_ENV}
        server-addr: ${NACOS_ADDR}
      config:
        username: nacos
        password: Hf@173503494
        namespace: frameworkjava-${RUN_ENV}
        server-addr: ${NACOS_ADDR}
        file-extension: yaml
        # 配置共享的配置
        shared-configs:
            # 可以这么拿到环境变量的值, 还有下面一种方法也可以
#          - data-id: share-redis-${RUN_ENV}.yaml
          - data-id: share-redis-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-caffeine-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-rabbitmq-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-token-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-filter-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-thread-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-mail-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-upload-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-idempotent-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-ratelimit-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新
          - data-id: share-log-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true # 配置是否支持动态刷新


# 这⼀配置用于定义应用在控制台输出日志时所遵循的格式规则
logging:
  pattern:
    console: '[%thread] %-5level %logger{36} - %msg%n'
  level:
    com.zmbdp.common.log.service.impl: debug
  file:
    name: ./logs/${spring.application.name}.log # 日志文件名
  logback:
    rollingpolicy:
      # 日志滚动策略
      file-name-pattern: ./logs/%d{yyyy-MM-dd}/${spring.application.name}-%d{yyyy-MM-dd}-%i.log
      max-file-size: 100MB # 日志文件最大大小
      max-history: 3 # 日志文件保留天数
      total-size-cap: 3GB # 日志文件总大小
      clean-history-on-start: false # 是否在启动时清理历史日志